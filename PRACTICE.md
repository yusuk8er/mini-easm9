# 演習：安全に試す

自社に向ける前に、**スキャンが許可されている場所**で練習します。

---

## 演習1：外部偵察を体験する（30分）

### 使う対象

`vulnweb.com` — Acunetix 社が教育目的での利用を明示的に許可しているテストサイトです。
サブドメインが複数あるので、EASM の「知らない資産が見つかる」感覚を体験できます。

### 手順

1. GitHub のリポジトリで `seeds.txt` を開き、**中身を全部消して**以下だけにする

```
vulnweb.com
```

2. AWS のキー（`AWS_ACCESS_KEY_ID` など）は **登録しないまま**にしておく
   → クラウド側はスキップされ、外部偵察だけが動きます

3. Actions タブ →「Run workflow」→ **診断の深さで `asset` を選んで実行**

4. 10分ほどで完了。`easm-result` をダウンロードして `assets.csv` を Excel で開く

### 見るべきポイント

- `seeds.txt` には1行しか書いていないのに、**複数のホストが見つかっている**
  → これが「サブドメイン列挙」です
- `tech` 列に古い技術が並んでいる（nginx の古いバージョン、PHP など）
  → これが「技術スタックの検出」です
- `cloud_provider` 列は空 → クラウドに紐づいていない資産、という判定です

### 次に、full モードで実行

同じ対象に対して、今度は `full` を選んで実行します。

- `risks.csv` が生成されます
- ポートスキャン・証明書検査・Web指摘の検出が行われます
- 実行時間が伸びます（30〜60分）
- `confidence` 列を見て、`confirmed` と `single` の違いを確認してください

> vulnweb.com は教育目的の利用が許可されていますが、
> 節度を持って使ってください。1日に何十回も回すような使い方はしないこと。

---

## 演習2：差分検知を体験する（15分）

EASM の本質は「差分」です。それを人工的に作って確認します。

1. 演習1が終わったら、`seeds.txt` に1行足す

```
vulnweb.com
scanme.nmap.org
```

2. もう一度 `asset` モードで実行

3. リポジトリの「Commits」を開き、最新のコミットをクリック

`assets.csv` の変更点が、**緑（追加）と赤（削除）で色分けされて表示**されます。
これが毎週自動で行われ、Slack に飛んでくる、という仕組みです。

> `scanme.nmap.org` は Nmap プロジェクトが
> 「スキャンしてよい」と公開しているホストです。

---

## 演習3：クラウド側を体験する（要AWS・60分）

ここは自分の AWS 検証アカウントが必要です。**本番アカウントでは絶対にやらないでください。**

### 準備

1. AWS の**検証用アカウント**（本番と分かれているもの）を用意する
2. `はじめかた.md` の手順3〜4に従って読み取りキーを登録する
3. `seeds.txt` は自分が持っているドメインだけにする

### わざと「見つかる資産」を作る

検証アカウントで、あえて外部公開のリソースを1つ作ります。

- EC2 を1台起動して、パブリックIPを付ける（t3.micro）
- または、S3 バケットを作ってパブリックアクセスを許可する

そのうえでスキャンを実行すると、`cloud.csv` と `assets.csv` に
そのリソースが出てくるはずです。**「作ったものが検出される」を確認する**のが目的です。

### 必ず後片付けをする

演習が終わったら、作ったリソースを**必ず削除**してください。

- EC2 を終了する
- S3 バケットのパブリックアクセスをブロックに戻す、またはバケットごと削除
- 使わなくなった IAM のアクセスキーを削除

パブリックにした S3 を放置すると、それ自体が本物のインシデントになります。

---

## 演習4：本番の前に、範囲を絞って試す

自社に向ける最初の1回は、**サブドメインが少ないドメイン1つ**から始めます。

1. 社内の監視チームに「これから ASM スキャンを試します」と連絡する
2. `seeds.txt` に対象ドメインを1つだけ書く
3. **まず `asset` モードで実行**
4. 出てきた `assets.csv` の行数を確認する

ここで想定より遥かに多い行数が出たら、それがすでに成果です。
「思っていたより公開資産が多い」という事実が、上司への説明材料になります。

---

## やってはいけないこと

- 許可されていない他社のサイトをスキャンする（不正アクセス禁止法）
- 「たぶん自社のだと思う」ドメインをスキャンする（確認してから）
- 業務時間帯に本番環境へ nuclei をフルで回す
- 演習で作ったパブリックな AWS リソースを放置する

---

## 詰まったら

エラーが出たら、Actions の画面で赤い×が付いたステップをクリックすると
ログが見られます。そのメッセージをコピーして相談してください。


---

## 演習5：機能ごとに適した演習場を使う

vulnweb.com は HTTPS も DNS も無いため、証明書検査・DNS検査・サービス識別が試せません。
以下はいずれも、運営者がスキャンを許可している公開の演習場です。

| 演習場 | 試せる機能 | 備考 |
|---|---|---|
| zonetransfer.me | DNSゾーン転送(AXFR)の検出 | DigiNinja が学習用に意図的にAXFRを許可 |
| scanme.nmap.org | ポートスキャン、サービス識別、SSH検出 | Nmapプロジェクトが許可。**1日に十数回まで** |
| ginandjuice.shop | HTTPS + Webの指摘検出 | PortSwigger がスキャナ検証用に公開 |
| badssl.com | 証明書の失効・自己署名・不一致 | **列挙してはいけない（下記）** |

### seeds.txt（列挙する対象）

```
zonetransfer.me
scanme.nmap.org
ginandjuice.shop
```

### hosts.txt（列挙せず直接指定する対象）

```
expired.badssl.com
self-signed.badssl.com
wrong.host.badssl.com
untrusted-root.badssl.com
tls-v1-0.badssl.com
```

### badssl.com を seeds.txt に書いてはいけない理由

badssl.com は「1枚の証明書に1万件のサブドメイン名を入れる」というテスト項目を
持っており、その証明書が証明書透明性ログに載っています。
subfinder はそこから14,000件以上のホスト名を拾ってしまい、
ポートスキャンが時間内に終わらなくなります。

このため `hosts.txt` に必要なホストだけを書いて、列挙を経由せずに調査します。

### 安全装置（止めずに自動調整します）

| 状況 | 動作 |
|---|---|
| 列挙が 5,000 件超（`MAX_SUBS`） | 先頭5,000件に絞って続行。捨てた件数をログに出す |
| 生存ホストが 300 件超（`MAX_LIVE`） | ポートスキャンを上位1000 → 上位100に自動で縮小 |

どちらも**スキャンは止まりません**。「多すぎて何も分からない」より
「一部でも結果が出る」ほうが実用的だからです。

上限を変えるには、ワークフローの scan ステップに環境変数を足します。

```yaml
      - name: scan
        env:
          MODE: ${{ github.event.inputs.mode || 'full' }}
          MAX_SUBS: 20000
          MAX_LIVE: 1000
```


---

## 演習6：クラウド側を検証する

クラウド側はドメインでは試せません。Steampipe が AWS/Azure/GCP の API を
直接呼ぶため、認証情報のあるアカウントが必要です。
「試せる公開アカウント」というものは存在しません。

ただし、**2段階に分けると1段目は無料・無リスク**で確認できます。

### 段階1：SQL のスキーマ検証（認証情報 不要・所要5分）

同梱の SQL に書かれたテーブル名・列名が、Steampipe の実際のスキーマと
合っているかを確認します。PostgreSQL はクエリ実行前に列の存在を検証するため、
認証情報が無くても誤りは検出できます。

**実行方法**: Actions → Run workflow → モードに `validate` と入力

```
==============================================
 azure
==============================================
  azure_public.sql         NG
      column "default_hostname" does not exist
```

このように出たら、その列名が間違っています。正しい列名は次で調べられます。

```bash
steampipe query "select column_name from information_schema.columns
                 where table_name = 'azure_app_service_function_app'
                 order by column_name"
```

Azure と GCP の SQL は一度も実行検証されていないため、
**クラウド側を使う前に必ずこれを実行してください。**

### 段階2：検証用アカウントで実際に検出させる（要AWS・所要1時間）

**本番アカウントでは絶対に行わないでください。**

1. AWS の検証用アカウントを用意する（無料利用枠で足ります）
2. `GETTING-STARTED.md` の手順3〜4に従って読み取りキーを登録
3. わざと検出される状態を作る

| 作るもの | 期待される検出 |
|---|---|
| S3バケットのパブリックアクセスを許可 | `s3-public-access` (critical) |
| セキュリティグループで 3389 を 0.0.0.0/0 に開放 | `sg-open-to-world` (critical) |
| EC2 を1台起動してパブリックIPを付与（t3.micro） | `cloud.csv` に ec2 として出現 |
| RDS を作らずとも上記2つで十分 | — |

4. `asset` モードで実行し、`cloud.csv` と `risks.csv` を確認
5. **必ず後片付けをする**
   - S3 のパブリックアクセスをブロックに戻す、またはバケット削除
   - セキュリティグループのルールを削除
   - EC2 を終了
   - 使い終わった IAM アクセスキーを削除

パブリックにした S3 を放置すると、演習が本物のインシデントになります。
作業ログを残し、その日のうちに片付けてください。

### 段階2をやらずに済ませる場合

段階1だけでも「SQLが壊れていない」ことは確認できます。
実際の検出精度は、本番の読み取り専用キーを登録して
`asset` モードで1回流せば分かります。読み取りしかしないので、
本番アカウントに対して実行しても副作用はありません。

（ただし IAM の権限付与自体は社内の承認手続きに従ってください）
