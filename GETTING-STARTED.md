# はじめかた（ブラウザだけで完結する手順）

Windows で、ターミナルを一度も使わずに動かす手順です。
PC には何もインストールしません。処理は GitHub のマシン上で走ります。

所要時間はだいたい 1〜2 時間（初回のみ）。

---

## 全体の流れ

```
0. スキャンの承認を取る              （最重要）
1. GitHub アカウントを作る          （10分）
2. ファイルを置く                   （10分）
3. AWS の読み取りキーを作る         （20分）
4. GitHub にキーを登録する          （5分）
5. 調べたいドメインを書く           （5分）
6. まず asset モードで実行する      （実行10〜20分）
7. 結果を確認してから full モードへ （実行30〜60分）
```

---

## 0. スキャンの承認を取る（最重要）

このツールは自社のサーバに対して以下を実行します。

- ポートスキャン（上位1000ポート）
- TLS証明書の検査
- 公開されているファイルの確認リクエスト

**攻撃と区別がつかない通信が発生します。** 必ず先に済ませてください。

1. **上司の承認を取る**（メールなど記録に残る形で）
2. **社内の監視チーム・SOCに連絡する**
   「毎週月曜の朝5時、GitHub Actions（Microsoft Azure の IP）から
   自社ドメイン向けの ASM スキャンが走ります」と伝える
3. **対象が本当に自社の資産か確認する**
   他社のサーバに向けると不正アクセス禁止法の問題になります

---

## 1. GitHub アカウントを作る

1. https://github.com を開く
2. 「Sign up」から アカウントを作る（無料）
3. 右上の「＋」→「New repository」
4. Repository name に `mini-easm` と入力
5. **「Private」を必ず選ぶ**（資産一覧が公開されると危険です）
6. 「Create repository」を押す

---

## 2. ファイルを置く

`README-SETUP.md` を参照してください。
このリポジトリはフォルダを使わないフラット構成なので、
ファイルをまとめてドラッグするだけです。

ワークフローファイル（`.github/workflows/scan.yml`）だけは
画面で作成する必要があります。手順は `README-SETUP.md` の手順3にあります。

## 3. AWS の読み取りキーを作る

AWS の管理画面（ブラウザ）で作業します。**読み取り専用**のキーを作ります。

1. AWS マネジメントコンソールにログイン
2. 検索窓に `IAM` と入れて開く
3. 左メニューの「ユーザー」→「ユーザーの作成」
4. ユーザー名: `mini-easm-readonly`
5. 「許可を設定」の画面で
   - 「ポリシーを直接アタッチする」を選ぶ
   - 検索窓に `SecurityAudit` と入れて、チェックを入れる
   - さらに `ViewOnlyAccess` にもチェックを入れる
6. 「ユーザーの作成」を押す
7. 作ったユーザーをクリック →「セキュリティ認証情報」タブ
8. 「アクセスキーを作成」→ 用途は「コマンドラインインターフェイス (CLI)」を選ぶ
9. 表示された **アクセスキー** と **シークレットアクセスキー** をコピーしておく

> `SecurityAudit` はセキュリティ設定の確認に特化した読み取り専用ポリシーです。
> `ReadOnlyAccess` でも動きますが、そちらは S3 の中身なども読めてしまうため、
> より権限の狭い `SecurityAudit` + `ViewOnlyAccess` を推奨します。

### 注意

- 書き込み権限を付けてはいけません
- このキーは AWS を操作できる重要な情報です。人に見せない、チャットに貼らない
- シークレットアクセスキーは画面を閉じると二度と見られません
- 使わなくなったら IAM の画面で削除してください

---

## 4. GitHub にキーを登録する

1. GitHub のリポジトリ画面 →「Settings」タブ
2. 左メニュー「Secrets and variables」→「Actions」
3. 「New repository secret」を押して、以下を1つずつ登録

| Name | Secret |
|---|---|
| `AWS_ACCESS_KEY_ID` | 手順3でコピーしたアクセスキー |
| `AWS_SECRET_ACCESS_KEY` | 手順3でコピーしたシークレットキー |

> Slack 通知が欲しい場合だけ `SLACK_WEBHOOK_URL` も追加してください。無くても動きます。

---

## 5. 調べたいドメインを書く

オンプレやレンタルサーバのドメインは、AWS から自動取得できないので手で書きます。

1. リポジトリ画面で `seeds.txt` をクリック
2. 右上の鉛筆マーク（Edit）を押す
3. 自社のドメインを1行ずつ書く

```
example.co.jp
example.com
```

4. 「Commit changes」を押す

> AWS の Route53 にあるドメインは自動で追加されるので、ここに書かなくても大丈夫です。
> **初回は1ドメインだけ**にすることを強く推奨します。

---

## 6. まず asset モードで実行する

1. リポジトリ画面の「Actions」タブを開く
2. 左メニューの「mini-easm」をクリック
3. 右側の「Run workflow」を押す
4. **「診断の深さ」で `asset` を選ぶ**
5. 「Run workflow」を押す

`asset` モードでは資産の棚卸しだけを行い、ポートスキャンも
脆弱性の確認リクエストも送りません。10〜20分で終わります。

### 結果を確認する

1. 実行が終わったら（緑のチェック）、その実行をクリック
2. ページ下の「Artifacts」にある `easm-result` をダウンロード
3. 解凍して `assets.csv` を Excel で開く

ここで確認すること：

- **想定していたドメインが対象になっているか**（他社のものが混ざっていないか）
- 行数が異常に多くないか
- `state` 列が `shadow`（持ち主不明）の行がどれくらいあるか

---

## 7. 問題なければ full モードへ

`asset` モードの結果に問題がなければ、`full` モードで診断を実行します。

1. 「Run workflow」→「診断の深さ」で `full` を選ぶ
2. 30〜60分かかります

`risks.csv` が追加で生成されます。

| confidence | 意味 | 扱い |
|---|---|---|
| `confirmed` | 観測事実、または2回照合を通過 | そのまま対応してよい |
| `single` | 1回だけの検出、WAF配下 | 人が確認してから判断 |

以降は毎週月曜の朝5時に `full` モードで自動実行されます。

---

## 8. 持ち主を記録していく

`shadow`（持ち主不明）の資産について「これ誰の?」と聞いて回り、
判明したものを `owners.yaml` に追記します。

1. リポジトリ画面で `owners.yaml` を開く → 鉛筆マーク
2. 1行ずつ追記する

```
www.example.co.jp: 広報チーム
api.example.co.jp: プラットフォーム部
```

3. 「Commit changes」

次回のスキャンから、そのホストは `shadow` ではなくなります。
**この作業の繰り返しが、EASM運用の実体です。**

---

## 結果を画面で見たいとき

`index.html` がビューワです。ブラウザで開くと
リスクと資産を切り替えて見られます。

ただしプライベートリポジトリでは GitHub Pages が使えないため
（Organization + Enterprise Cloud が必要）、
リポジトリを ZIP でダウンロードして、`index.html` をブラウザで開いてください。

> `assets.csv` / `risks.csv` が読み込めない場合はサンプルデータが表示されます。
> 実データを見るには簡易サーバが必要になるので、
> まずは CSV を Excel で開くほうが手軽です。

---

## エラーが出たとき

赤い×が付いたら、その実行をクリックすると、どこで止まったかが見られます。

| よくあるエラー | 原因と対処 |
|---|---|
| `Unable to locate credentials` | 手順4のキー登録ができていない。名前のスペルを確認 |
| `AccessDenied` | IAM の権限不足。`SecurityAudit` が付いているか確認 |
| `シードが空です` | 手順5で `seeds.txt` にドメインを書けていない |
| `permission denied` | スクリプトに実行権限がない。詳しい人に相談 |
| naabu が失敗する | `libpcap-dev` の導入で失敗している可能性。ログを確認 |

---

## 正直な話

ここまでの手順は、ターミナル未経験の方には**それなりに歯応えがあります**。
特に手順3の IAM 周りは、間違えると権限を与えすぎる事故にもつながります。

以下に当てはまるなら、無理せず別の道を検討したほうがいいかもしれません。

- 社内にコマンドライン操作に慣れた人がいない
- AWS の IAM を触った経験がない
- エラーが出たときに自力で調べる時間がない

その場合は、

1. **社内のエンジニアに手順3〜4だけ手伝ってもらう**（30分で済みます）
2. **商用の ASM サービスを使う**

を検討してください。
このツールの本当のコストは「無料のOSS」ではなく、**動かし続ける人の時間**です。
