# はじめかた（ブラウザだけで完結する手順）

Windows で、ターミナルを一度も使わずに動かす手順です。
PC には何もインストールしません。処理は GitHub のマシン上で走ります。

所要時間はだいたい 1 時間（初回のみ）。

---

## 全体の流れ

```
0. スキャンの承認を取る              （最重要）
1. GitHub アカウントを作る          （10分）
2. ファイルを置く                   （10分）
3. 調査対象ドメインを書く           （5分）
4. まず asset モードで実行する      （実行10〜20分）
5. 結果を確認してから full モードへ （実行30〜60分）
```

---

## 0. スキャンの承認を取る（最重要）

このツールは自社のサーバに対して以下を実行します。

- ポートスキャン（上位1000ポート）
- TLS証明書の検査
- 公開されているファイルの確認リクエスト

**攻撃と区別がつかない通信が発生します。** 必ず先に済ませてください。

1. **上司の承認を取る**（メールなど記録に残る形で）
2. **社内の監視チーム・SOCに連絡する**
   「毎週月曜の朝5時、GitHub Actions（Microsoft Azure の IP）から
   自社ドメイン向けの ASM スキャンが走ります」と伝える
3. **対象が本当に自社の資産か確認する**
   他社のサーバに向けると不正アクセス禁止法の問題になります

> 関連ドメインの探索（discover モード）は証明書透明性ログを読むだけで、
> 対象サーバへの通信が発生しません。承認前でも実行できる場合があります。

---

## 1. GitHub アカウントを作る

1. https://github.com を開く
2. 「Sign up」からアカウントを作る（無料）
3. 右上の「＋」→「New repository」
4. Repository name を入力
5. **「Private」を必ず選ぶ**（資産一覧が公開されると危険です）
6. 「Create repository」を押す

---

## 2. ファイルを置く

`README-SETUP.md` を参照してください。
このリポジトリはフォルダを使わないフラット構成なので、
ファイルをまとめてドラッグするだけです。

ワークフローファイル（`.github/workflows/scan.yml`）だけは
画面で作成する必要があります。手順は `README-SETUP.md` の手順3にあります。

---

## 3. 調査対象ドメインを書く

1. リポジトリ画面で `seeds.txt` をクリック
2. 右上の鉛筆マーク（Edit）を押す
3. 自社のドメインを1行ずつ書く

```
example.co.jp
example.com
```

4. 「Commit changes」を押す

> **初回は1ドメインだけ**にすることを強く推奨します。

列挙では見つからないホストが分かっている場合は、`hosts.txt` に書きます。

---

## 4. まず asset モードで実行する

1. リポジトリ画面の「Actions」タブを開く
2. 左メニューの「mini-easm」をクリック
3. 右側の「Run workflow」を押す
4. **モードに `asset` を入力**
5. 「Run workflow」を押す

`asset` モードでは資産の棚卸しだけを行い、ポートスキャンも
確認リクエストも送りません。10〜20分で終わります。

### 結果を確認する

1. 実行が終わったら（緑のチェック）、その実行をクリック
2. ページ下の「Artifacts」にある `easm-result` をダウンロード
3. 解凍して `assets.csv` を Excel で開く

ここで確認すること：

- **想定していたドメインが対象になっているか**（他社のものが混ざっていないか）
- 行数が異常に多くないか
- `state` 列が `shadow`（持ち主不明）の行がどれくらいあるか
- ログに「DNS上は存在するが無応答のホスト」の警告が出ていないか

---

## 5. 問題なければ full モードへ

`asset` モードの結果に問題がなければ、`full` モードで診断を実行します。

1. 「Run workflow」→ モードに `full` を入力
2. 30〜60分かかります

`risks.csv` が追加で生成されます。

| confidence | 意味 | 扱い |
|---|---|---|
| `confirmed` | 観測事実、または2回照合を通過 | そのまま対応してよい |
| `single` | 1回だけの検出、WAF配下 | 人が確認してから判断 |

以降は毎週月曜の朝5時に `full` モードで自動実行されます。

---

## 6. 持ち主を記録していく

`shadow`（持ち主不明）の資産について「これ誰の?」と聞いて回り、
判明したものを `owners.yaml` に追記します。

```
www.example.co.jp: 広報チーム
api.example.co.jp: プラットフォーム部
```

次回のスキャンから、そのホストは `shadow` ではなくなります。
**この作業の繰り返しが、EASM運用の実体です。**

---

## 7. 知らないドメインを探す（任意）

`seeds.txt` に書いたドメインの配下しか探索しないため、
別ドメインで取得された資産は見つかりません。

`discover` モードで実行すると、証明書透明性ログから
把握できていないドメインの候補を洗い出します。

結果は `candidates.csv` に出力されます。
**1件ずつ自社のものか確認し、確認できたものだけを `seeds.txt` に追記してください。**

---

## 結果を画面で見たいとき

アーティファクトの中に **`report.html`** が入っています。
**ダウンロードしてダブルクリックするだけ**で開けます。

スキャン結果がHTMLに埋め込まれているため、サーバもインストールも不要です。
そのままメールに添付して共有することもできます。

- 上部のボタンで「リスク」と「資産」を切り替え
- 深刻度や持ち主で絞り込み
- 画面下にスキャン日時を表示

リポジトリのルートにも同じものがコミットされます。

---

## エラーが出たとき

赤い×が付いたら、その実行をクリックすると、どこで止まったかが見られます。

| よくあるエラー | 原因と対処 |
|---|---|
| `調査対象がありません` | `seeds.txt` にドメインを書けていない |
| `Invalid workflow file` | ワークフローの貼り付けが崩れている |
| `nmap: なし` | パッケージ導入に失敗。ログの `install system packages` を確認 |
| `permission denied` | スクリプトに実行権限がない |

スキャン結果の ZIP には `debug/` フォルダが含まれます。
ツールの導入状況やエラー出力が入っているので、原因の切り分けに使えます。
