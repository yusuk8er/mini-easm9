# 誤検知をどう抑えているか

外部からのスキャンで誤検知が出る原因はほぼ4つに集約されます。
それぞれに対策を設計へ組み込んでいます。

---

## 原因1：バージョンから推測して判定する

**よくある誤検知**

> バナーに `Apache/2.4.7` と出ている
> → Apache 2.4.7 に該当するCVEを全部リストアップ
> → 実際はディストリのバックポートパッチ適用済みで影響なし

これが外部スキャンの誤検知の最大の発生源です。日本のディストリや
マネージドサービスはバージョン番号を据え置いたままパッチを当てるため、
バージョン一致による判定はほぼ当たりません。

**対策：推測型の判定を一切しない**

`cpe` 列は出力しますが、**そこからCVEを引く処理は入れていません。**
使い道は「緊急CVE公表時に調査対象を絞る」ことに限定しています。

```bash
# nginx に緊急CVEが出た → 調べるべき資産はどれか
grep -i 'cpe:2.3:a:nginx' snapshots/assets.csv | cut -d, -f1
```

「該当しうる資産の一覧」であって「脆弱な資産の一覧」ではありません。
実際に脆弱かどうかは、資産の担当者に確認するか、
内側から構成情報を取る方式（エージェント/SSH）で確定させてください。

---

## 原因2：マッチ条件が緩い

**よくある誤検知**

- 404ページの本文にたまたまキーワードが含まれていた
- WAFのブロックページを「検出結果」と誤認した
- ログイン画面へリダイレクトされた先の内容を判定してしまった

**対策1：テンプレート方式のツールだけを使う**

nuclei は「どのリクエストを送り、どのレスポンスパターンなら問題か」を
テンプレートで明示する方式です。総当たりで異常を探すのではなく、
既知の条件に対する照合なので、構造的に誤検知が出にくくなっています。

**対策2：誤検知の多いカテゴリを明示的に除外**

`nuclei-config.yaml` で以下を除外しています。

| 除外タグ | 理由 |
|---|---|
| `fuzz` `dast` | パラメータ総当たり。誤検知の温床 |
| `xss` `sqli` | 反射型の判定は誤検知が多い |
| `dos` `intrusive` `brute` | 相手に負荷・被害を与える |
| `info` | 情報レベルはノイズにしかならない |

**対策3：リダイレクトを追わない**

`disable-redirects: true` を設定しています。
ログイン画面に飛ばされた結果を「検出」とみなす誤検知を防ぎます。

---

## 原因3：一過性の応答を拾ってしまう

レート制限、一時的なエラー、ロードバランサ配下の特定インスタンスだけの
挙動などで、たまたま1回だけ検出されることがあります。

**対策：2回照合する**

1回目の検出結果に対して、**同じテンプレート・同じ対象で2回目を実行**し、
両方に出たものだけを `confirmed` としています。

```
1回目で検出 → 5秒待つ → 同じ条件で再実行
  ├─ 2回とも出た  → confirmed（確認済み）
  └─ 1回だけ出た  → single（要確認）
```

Slack通知に出るのは `confirmed` だけです。`single` は件数だけ通知し、
中身は画面で人が確認する運用にしています。

---

## 原因4：WAF・CDNが応答を歪める

WAFの背後にあるホストは、実際のアプリの応答ではなく
WAFの応答を見ていることがあります。

**対策：WAF/CDN配下は確度を1段階下げる**

httpx の `-cdn` でWAF/CDNを検出し、その配下のホストの指摘は
`confirmed` にせず `single` に落としています。

---

## 確度のランク

出力される `risks.csv` の `confidence` 列は2段階です。

### confirmed（確認済み）— そのまま対応してよい

観測できた事実、または2回照合を通過したものです。

| 検出元 | 内容 | なぜ誤検知しないか |
|---|---|---|
| naabu | 開いているポート | TCP接続が成立した事実 |
| tlsx | 証明書の失効・自己署名・ホスト名不一致 | 証明書の中身を読んだ結果 |
| tlsx | 古いTLSバージョンが有効 | ハンドシェイクが成立した事実 |
| dnsx | CNAME先が存在しない（乗っ取り可能） | 名前解決の結果 |
| nuclei | 2回とも検出された指摘 | 実レスポンスで2回確認 |

### single（要確認）— 人が見てから判断する

nuclei が1回だけ検出したもの、またはWAF配下のものです。
**誤検知の可能性があるという前提で扱ってください。**

---

## それでも残る誤検知

正直に書いておきます。以下は原理的に避けられません。

**「検出は正しいが、リスクではない」ケース**

- 意図的に公開している管理画面（IP制限済み）
- 検証環境の自己署名証明書
- 外部公開が仕様のFTPサーバ

これは技術で判定できないので、**例外リストを作って運用で消し込む**しかありません。
`owners.yaml` と同じ発想で、判断済みのものを記録していく形になります。

必要になったら `accepted-risks.yaml`（許容済みリスクの一覧）を追加します。
最初は不要です。実際に運用してみて、繰り返し出てくるものが見えてから作るほうが、
無駄なファイルを持たずに済みます。

---

## 逆に「見逃す」ほうのリスク

誤検知を減らすと、その分だけ見逃しが増えます。この構成が捨てているもの：

- 認証が必要な画面の内側（ログインしないと分からない脆弱性）
- ビジネスロジックの欠陥
- XSS / SQLインジェクション（誤検知が多いため除外している）
- バージョン起因の既知脆弱性（推測を排除したため）

**これらが必要なら、外形スキャンではなく本格的な脆弱性診断か、
内側から構成情報を取る方式（エージェント/SSH）が適切です。**

この構成の役割は「外から見える明らかな穴を、誤報なく拾い続けること」です。
そこに徹する代わりに、深さは諦めています。
